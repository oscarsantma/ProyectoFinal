<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/gris_fondo">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/verde_andalucia_primary"
        android:padding="20dp">

        <TextView
            android:id="@+id/tvTituloResidentes"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Residentes de la residencia"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/blanco" />

        <TextView
            android:id="@+id/btnNuevoResidente"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Añadir"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@color/blanco"
            android:background="#33FFFFFF"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvResidentesResidencia"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp"
        android:clipToPadding="false" />

    <TextView
        android:id="@+id/tvSinResidentes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="No hay residentes cargados en esta residencia."
        android:textColor="@color/gris_texto"
        android:textSize="14sp"
        android:gravity="center"
        android:visibility="gone" />

    <TextView
        android:id="@+id/btnVolverResidentes"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:text="Volver"
        android:textSize="14sp"
        android:textColor="@color/gris_texto"
        android:gravity="center"
        android:textStyle="bold"
        android:background="@drawable/bg_card"
        android:clickable="true"
        android:focusable="true"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="24dp" />
</LinearLayout>
